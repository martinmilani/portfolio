---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`pt-16 scroll-m-20 w-full mx-auto container lg:max-w-4xl md:max-w-2xl ${className}`}
>
  <slot />
</section>
